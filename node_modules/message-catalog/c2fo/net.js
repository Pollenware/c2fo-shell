var MCatCommon = require( 'message-catalog/common' ).MCatCommon;

exports.MCat = new MCat();

function MCat () {

  this.agentDefault = 'C2FO HTTP client';
  this.errorC2FONet = 'C2FO network error';
  this.passwordMask = '-';

  for ( a in MCatCommon) {
    if ( a in this ) { continue; }
    this[a] = MCatCommon[a];
  }
}
